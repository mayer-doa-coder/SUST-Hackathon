apiVersion: v1
kind: Service
metadata:
  name: queuestorm-gateway
  namespace: queuestorm
spec:
  type: ClusterIP
  selector:
    app: queuestorm-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8000
